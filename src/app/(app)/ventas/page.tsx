import { VentasLista } from "@/components/ventas/VentasLista";

/**
 * P9 · Ventas y oportunidades (M5.2 · TAL-50).
 * Diseño: ruta `ventas` en design/PROY CRM Pulse/CRM Pulse.dc.html (no hay archivo standalone).
 * KPIs En marcha / Ganado, filtros por estado/cliente/periodo, columna vendedor, ⋮ Editar/Eliminar.
 */
export default function VentasPage() {
  return <VentasLista />;
}
