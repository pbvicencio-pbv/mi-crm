/**
 * P9 · Ventas y oportunidades (M5.2 · TAL-50).
 * Diseño: ruta `ventas` en design/PROY CRM Pulse/CRM Pulse.dc.html (no hay archivo standalone).
 * KPIs En marcha / Ganado, filtros por estado/cliente/periodo, columna vendedor.
 */
export default function VentasPage() {
  return (
    <section>
      <h1 className="text-2xl font-extrabold tracking-tight">Ventas</h1>
      <p className="mt-1 text-sm text-slate-500">
        Placeholder — Ventas y oportunidades (TAL-50).
      </p>
    </section>
  );
}
