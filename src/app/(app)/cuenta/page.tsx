/**
 * P10 · Perfil / Mi cuenta (M2.4 · TAL-48).
 * Diseño: design/PROY CRM Pulse/CuentaPerfil.screen.dc.html (+ .gallery).
 * Perfil (nombre, correo, rol), editar datos, cambiar contraseña, (dueña) acceso a Equipo, cerrar sesión.
 */
export default function CuentaPage() {
  return (
    <section>
      <h1 className="text-2xl font-extrabold tracking-tight">Mi cuenta</h1>
      <p className="mt-1 text-sm text-slate-500">Placeholder — Perfil / Mi cuenta (TAL-48).</p>
    </section>
  );
}
