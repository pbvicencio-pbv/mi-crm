/**
 * P5 · Nuevo / editar cliente (M3.1 · TAL-11; unificar/ampliar según TAL-58).
 * Diseño: design/PROY CRM Pulse/ClienteForm.screen.dc.html (+ .gallery).
 * Captura TODOS los campos de la ficha + selector de prioridad; estado NO editable (auto).
 */
export default function NuevoClientePage() {
  return (
    <section>
      <h1 className="text-2xl font-extrabold tracking-tight">Nuevo cliente</h1>
      <p className="mt-1 text-sm text-slate-500">Placeholder — Alta/edición de cliente (TAL-11).</p>
    </section>
  );
}
