/**
 * P11 · Gestión de usuarios / Equipo (M2.3 · TAL-32). SOLO rol "duena".
 * Diseño: ruta `equipo` + overlay `usuario` en design/PROY CRM Pulse/CRM Pulse.dc.html.
 * Alta/edición/eliminación y cambio de rol, con límites (mín. una dueña; no auto-eliminarse).
 */
export default function EquipoPage() {
  return (
    <section>
      <h1 className="text-2xl font-extrabold tracking-tight">Equipo</h1>
      <p className="mt-1 text-sm text-slate-500">
        Placeholder — Gestión de usuarios, solo dueña (TAL-32).
      </p>
    </section>
  );
}
