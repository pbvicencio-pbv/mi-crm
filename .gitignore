# dependencias
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# producción / logs
*.log
npm-debug.log*

# entorno (los secretos reales NO se commitean)
.env
.env*.local
!.env.local.example

# typescript / next
*.tsbuildinfo
next-env.d.ts

# sistema
.DS_Store
Thumbs.db

# archivos comprimidos del paquete de diseño (la carpeta extraída sí se versiona)
*.zip

# convex: la carpeta convex/_generated SÍ se versiona (tipos generados);
# los secretos locales viven en .env.local

# claude code: config local por máquina (CLAUDE.md, AGENTS.md y .claude/memory SÍ se versionan)
.claude/settings.local.json

# temporales de trabajo
/tmp/
