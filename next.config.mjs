/** @type {import('next').NextConfig} */
const nextConfig = {
  // El MVP es web mobile-first sobre Next.js 15 (App Router).
  // Backend/datos en Convex; despliegue en Railway.
  reactStrictMode: true,
};

export default nextConfig;
